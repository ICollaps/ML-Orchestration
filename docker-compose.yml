version: '3.8'

services:
  api:
    build:
      context: ./model_api
    container_name: api
    ports:
      - "8080:8080"
    volumes:
      - ./model_api:/app
    environment:
      - MODEL_PATH=/app/knn_model.pkl
